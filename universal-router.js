// This is the input file for the 'universal-router' gulp task.
// The gulp task generates a browser-ready lib/universal-router.js file from the 'universal-router' node package.
import UniversalRouter from 'universal-router/src/UniversalRouter.js';

window.UniversalRouter = UniversalRouter;
